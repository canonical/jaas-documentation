(tag)=
# Tag

In JAAS, in the context of Relation Based Access Control (**ReBAC**), a tag is a string that uniquely identifies a resource.

A tag consists of the resource type and the resource identifier.

> See more: {ref}`controller-tag`, {ref}`cloud-tag`, {ref}`model-tag`, {ref}`offer-tag`, {ref}`user-tag`, {ref}`role-tag`, {ref}`group-tag`




